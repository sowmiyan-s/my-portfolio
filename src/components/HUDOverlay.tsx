// HUDOverlay — intentionally empty.
// The previous full-screen diagnostic text (FPS, coordinates, location, build info)
// has been removed for a cleaner user experience.

const HUDOverlay = () => null;

export default HUDOverlay;
